namespace :automator do
  desc "Installs Standard Automator Boilplate - Devise, Bootstrap Gem, SimpleForm. Note this will replace all files it touches, so please use only with a newly generated app."
  task :install do
    puts "I was run"
  end
end
